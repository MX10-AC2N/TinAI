server {
    listen 8080;

    # Dashboard statique
    location / {
        root /usr/share/nginx/html;
        index index.html;
    }

    # Proxy API métriques → api.py
    location /api/ {
        proxy_pass http://127.0.0.1:8888;
        proxy_read_timeout 10s;
    }
}
